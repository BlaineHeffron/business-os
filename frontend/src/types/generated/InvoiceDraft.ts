// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { DraftFieldProvenance } from "./DraftFieldProvenance";
import type { InvoiceDraftLineItem } from "./InvoiceDraftLineItem";
import type { InvoiceDraftStatus } from "./InvoiceDraftStatus";

export type InvoiceDraft = {
/**
 * "inv_<item_id>_<attempt>" — one active (non-rejected) draft per item.
 */
draft_id: string, item_id: string, source_kind: string, source_ref: string, status: InvoiceDraftStatus,
/**
 * Who gets billed (Stripe customer name).
 */
customer_name: string,
/**
 * Required for approval — Stripe's find-or-create key.
 */
customer_email?: string | null,
/**
 * ISO 4217 (v1 stages "usd").
 */
currency: string, line_items: Array<InvoiceDraftLineItem>,
/**
 * Recomputed server-side from the line items; equals total in v1
 * (no tax/discount lines yet).
 */
subtotal_cents: number, total_cents: number,
/**
 * YYYY-MM-DD, only when the source states one (never invented).
 */
due_date?: string | null,
/**
 * Lands on the Stripe invoice's description/memo field.
 */
memo: string, provenance: Array<DraftFieldProvenance>, model: string, confidence: string, outbox_job_id?: string | null, created_at_ms: number, updated_at_ms: number, };
