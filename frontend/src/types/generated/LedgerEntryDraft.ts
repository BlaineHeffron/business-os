// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { DraftFieldProvenance } from "./DraftFieldProvenance";
import type { LedgerDraftStatus } from "./LedgerDraftStatus";

export type LedgerEntryDraft = {
/**
 * "led_<item_id>_<attempt>" — one active (non-rejected) draft per item.
 */
draft_id: string, item_id: string, source_kind: string, source_ref: string, status: LedgerDraftStatus,
/**
 * Who paid (client/customer name in the accounting system).
 */
payer_name: string, payer_email?: string | null,
/**
 * Integer cents — REQUIRES a literal provenance quote from the source.
 */
amount_cents: number,
/**
 * YYYY-MM-DD; defaults to the source email's date when the source
 * doesn't state one explicitly (grounded, never model-invented).
 */
paid_date: string,
/**
 * What the payment was for (the invoice line / payment note).
 */
description: string, provenance: Array<DraftFieldProvenance>, model: string, confidence: string, outbox_job_id?: string | null, created_at_ms: number, updated_at_ms: number, };
