// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { CalendarDraftStatus } from "./CalendarDraftStatus";
import type { DraftFieldProvenance } from "./DraftFieldProvenance";

export type CalendarEventDraft = {
/**
 * "ced_<item_id>_<attempt>" — one active (non-rejected) draft per item.
 */
draft_id: string, item_id: string, source_kind: string, source_ref: string,
/**
 * Operator user this draft is bound to, inherited from the originating
 * work item. Null = legacy rows / all-scope-only.
 */
source_user_id?: string | null, status: CalendarDraftStatus, title: string,
/**
 * RFC3339 timestamps (what the Google Calendar API consumes).
 */
start_at: string, end_at: string, timezone?: string | null, location?: string | null, description?: string | null,
/**
 * Calendar the approved event writes to. None = the server default
 * (BOS_GOOGLE_CALENDAR_ID). Operator-editable while staged, picked from
 * the connected account's writable calendars.
 */
calendar_id?: string | null, attendees: Array<string>, send_invitations: boolean, provenance: Array<DraftFieldProvenance>,
/**
 * Model that produced the extraction (audit display).
 */
model: string,
/**
 * Extractor's own confidence: "high" | "medium" | "low".
 */
confidence: string,
/**
 * Set when approved: the outbox job carrying the provider write.
 */
outbox_job_id?: string | null, created_at_ms: number, updated_at_ms: number, };
